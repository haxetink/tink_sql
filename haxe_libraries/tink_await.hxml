# @install: lix --silent download "haxelib:tink_await#0.2.0" into tink_await/0.2.0/haxelib
-D tink_await=0.2.0
-cp ${HAXESHIM_LIBCACHE}/tink_await/0.2.0/haxelib/src
--macro tink.await.Await.use()
-lib tink_macro
-lib tink_core
-lib tink_syntaxhub